import { Component } from '@angular/core';

@Component({
  selector: 'app-preview',
  standalone: true,
  template: `<section class="card"><h1>Preview Hub</h1><p class="small">Vista unificada de previews (stub).</p></section>`
})
export class PreviewComponent {}
